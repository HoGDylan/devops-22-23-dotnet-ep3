<!--@using Microsoft.AspNetCore.Components.Authorization-->
@using Microsoft.AspNetCore.Components.Web

@inject NavigationManager Navigation
<!--
<AuthorizeView>
    <Authorized>
        <button class="button is-light" @onclick="BeginSignOut">
            <strong>Uitloggen</strong>
        </button>
    </Authorized>
    <NotAuthorized>
        <a class="button is-primary" @onclick="BeginSignIn">
            <strong>Inloggen</strong>
        </a>
    </NotAuthorized>
</AuthorizeView>
-->
<button class="button is-light" @onclick="BeginSignOut">
    <strong>Uitloggen</strong>
</button>
<a class="button is-primary" @onclick="BeginSignIn">
    <strong>Inloggen</strong>
</a>

@code { private Task BeginSignOut(MouseEventArgs args)
    {
        throw new NotImplementedException("Logout is currently not supported.");
    }
    private Task BeginSignIn(MouseEventArgs args)
    {
        throw new NotImplementedException("Login is currently not supported.");
    } }