<div class="top-row ps-3 navbar navbar-dark">
    <h3 class="title is-4 logo" href="https://www.hogent.be/">Virtual IT Company</h3>
    <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
        <span class="navbar-toggler-icon"></span>
    </button>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link subtitle is-6" href="virtualmachines" Match="NavLinkMatch.All">
                <span class="oi oi-monitor" aria-hidden="true"></span> Virtual Machines
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link subtitle is-6" href="klanten">
                <span class="oi oi-people" aria-hidden="true"></span> Klanten
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link subtitle is-6" href="beheerders">
                <span class="oi oi-key" aria-hidden="true"></span> Beheerders
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="virtualmachines/add">
                <span class="oi oi-plus" aria-hidden="true"></span> VirtualMachines
            </NavLink>
        </div>
    </nav>
</div>


@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
